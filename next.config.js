module.exports = {
  output: "export", // Static export configuration
  trailingSlash: true, // Ensure URLs have trailing slashes
};

// This configuration is for a Next.js project that exports static files.
// The `output: "export"` option indicates that the project will be exported as static files
